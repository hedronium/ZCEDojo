---
question: |
    ```PHP
    class A {}
    $a = new A;
    $b = clone $a;

    echo $a===$b ? 'YEAH!' : 'NO';
    ```
    What is the output?

options:
    - |
        "YEAH!"

    - |
        "NO"

    - |
        There will be a systax error.

    - |
        There will be an undefined constant error.

correct: 1